var assert = require('assert')

describe('Authorization', () => {
  it('should accept a valid auth header')
  it('should not accept empty auth headers')
  it('should return a login page if trying to login')
})
